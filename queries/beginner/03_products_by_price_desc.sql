/*
Problem 03: Products by Price (Descending)

Objective:
Retrieve the ProductID, ProductName, and UnitPrice of all products, sorted by UnitPrice in descending order.

Concepts Used:
- SELECT
- ORDER BY
*/
SELECT ProductID, ProductName, UnitPrice
FROM Products
ORDER BY UnitPrice DESC;
